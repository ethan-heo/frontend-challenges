{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "showcase:build": {
      "outputs": ["dist/**"],
      "cache": false
    },
    "build": {
      "dependsOn": ["showcase:build"],
      "outputs": ["dist/**"],
      "cache": false
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}

{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "showcase:build": {
      "outputs": ["dist/**"]
    },
    "build": {
      "dependsOn": ["showcase:build"],
      "outputs": ["dist/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
